[project]
name = "event-radar"
version = "0.1.0"
description = "個人化藝文活動雷達 (personal taste-based event radar)"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "pyyaml>=6.0",
    "streamlit>=1.36",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["event_radar*"]
